{
	"name": "kick-counter",
	"version": "1.0.0",
	"description": "a groupme bot to count kicks",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:start": "nodemon -r dotenv/config server.js",
		"start": "PORT=3000 node -r dotenv/config server.js"
	},
	"author": "",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"axios": "^0.20.0",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"nodemon": "^2.0.5"
	},
	"nodemonConfig": {
		"ignore": [
			"/scores.json",
			"/members.json"
		]
	}
}
